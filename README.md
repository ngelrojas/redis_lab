#### redis_lab for localhost

#### run
```bash
  docker-compose up -d
```

#### in docker
```bash
  docker exec -it redis-container bash
```

#### redis-cli
- in docker run redis cli
```bash
  redis-cli
```
